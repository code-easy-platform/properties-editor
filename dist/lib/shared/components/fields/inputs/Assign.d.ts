import { IProperty } from './../../../interfaces';
import { default as React } from 'react';

interface IAssignProps extends IProperty<string> {
    inputClassName?: string;
}
export declare const Assign: React.FC<IAssignProps>;
export {};
