import { IProperty } from '../../../interfaces';
import { default as React } from 'react';

interface InputSelectionProps extends IProperty<string> {
}
export declare const InputSelection: React.FC<InputSelectionProps>;
export {};
